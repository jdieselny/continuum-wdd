{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://wdd.jdieselny.com/schemas/agent.schema.json",
  "title": "Agent Enrollment",
  "description": "Agent enrollment record with cryptographic identity.",
  "type": "object",
  "properties": {
    "agent_id": {
      "type": "string",
      "description": "The unique identifier of the agent."
    },
    "public_key": {
      "type": "string",
      "description": "The public key of the agent, formatted for Ed25519."
    },
    "capabilities": {
      "type": "array",
      "description": "Array of grid squares.",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "agent_id",
    "public_key",
    "capabilities"
  ]
}
